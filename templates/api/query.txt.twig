{% if type == constant('App\\Controller\\ApiController::QUERY_IP') %}
  Results of IP address lookup for {{ query }}:
  {% for item in data -%}
    {{ item.callsign }} ({{ item.times }} times)
  {% else -%}
    No results found
  {% endfor %}
{% elseif type == constant('App\\Controller\\ApiController::QUERY_CMD') %}

{% elseif type == constant('App\\Controller\\ApiController::QUERY_CALLSIGN') %}

{% else %}

{% endif %}
